<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Riya</title>
    <style>
        body {
            text-align: center;
            background-color: #fce4ec;
        }
        .cake {
            margin-top: 50px;
        }
        .balloon {
            position: absolute;
            bottom: -100px;
            width: 50px;
            animation: float 10s linear infinite;
        }
        @keyframes float {
            0% { transform: translateY(0) }
            100% { transform: translateY(-100vh) }
        }
    </style>
</head>
<body>
    <h1>Happy Birthday Akash!</h1>
    <img src="YOUR_IMAGE_URL_HERE" alt="Birthday Cake for Riya" class="cake" width="400">

    <script>
        function createBalloon() {
            const balloon = document.createElement('img');
            balloon.src = 'https://path/to/balloon-image.png'; // Replace with a balloon image URL
            balloon.classList.add('balloon');
            balloon.style.left = Math.random() * window.innerWidth + 'px';
            document.body.appendChild(balloon);
            
            setTimeout(() => balloon.remove(), 10000);
        }

        setInterval(createBalloon, 500);
    </script>
</body>
</html>
